/*
estructuras

En este fichero crearemos las struct necesarias
*/

struct persona{
    int edad, estado, tiempo, riesgo;
    int posicion[2], velocidad[2];
};

struct poblacion{
    int per_max, radio_con, periodo_inc, periodo_rec, vel_max, vac_nec, probabilidad_cont, cambiovel, limite;
};

